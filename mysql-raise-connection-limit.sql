-- ============================================
-- رفع حد اتصالات MySQL بالساعة (max_connections_per_hour)
-- ============================================
-- ⚠️ على الاستضافة المشتركة (Hostinger وغيرها) لا يمكن تنفيذ هذا الملف — يظهر:
--    #1227 - Access denied; you need CREATE USER privilege(s)
-- الحل الوحيد على الاستضافة: تقليل استهلاك الاتصالات من التطبيق (انظر أسفل الملف).
--
-- هذا الملف للمستخدمين الذين يملكون صلاحيات مدير MySQL (VPS / سيرفر خاص فقط):
-- ============================================

-- لسيرفر خاص أو VPS فقط — عدّل اسم المستخدم والعنوان إن لزم
-- ALTER USER 'u486977009_academyuserv1'@'%'
--   WITH MAX_CONNECTIONS_PER_HOUR 10000
--        MAX_QUERIES_PER_HOUR 0
--        MAX_UPDATES_PER_HOUR 0
--        MAX_USER_CONNECTIONS 0;
-- FLUSH PRIVILEGES;

-- ============================================
-- على الاستضافة المشتركة (500 اتصال/ساعة): الحل من التطبيق فقط
-- ============================================
-- 1) في api/.env تأكد من: DB_PERSISTENT=true
-- 2) التطبيق يقلل الاستدعاءات تلقائياً: تخزين مؤقت لـ verify (5 دقائق)، تتبع زيارة مرة/صفحة/جلسة، تقليل استدعاءات لوحة الأدمن.
-- 3) إن استمر الخطأ: اطلب من دعم الاستضافة رفع الحد (هم وحدهم يملكون الصلاحية).
