name: Deploy to Thamara Cloud

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.amrnayl.online
          username: u486977009.amrnayl
          password: NYgLGBav&jz&Qa95
          protocol: ftp
          port: 21
          security: loose
          timeout: 120000
          log-level: standard

          server-dir: /an/
          # مهم: لا يحذف ملفات السيرفر 
          dangerous-clean-slate: false

          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/.env
            **/.env.*
            **/.vscode/**
            **/.idea/**
            **/README.md
            **/*.log
            **/backups/**
            **/temp/**
            **/tmp/**
            **/.DS_Store
            **/Thumbs.db
            .gitignore
            .editorconfig

